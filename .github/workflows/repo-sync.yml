# File: .github/workflows/repo-sync.yml

on:
  schedule:
  - cron:  "*/15 * * * *"
  workflow_dispatch:

jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        persist-credentials: false
    - name: repo-sync
      uses: repo-sync/github-sync@v2
      with:
        source_repo: ""
        source_branch: "https://gitcode.net/qq_32394351/dr_py"
        destination_branch: "https://github.com/cfsxy/dr_py"
        github_token: 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
